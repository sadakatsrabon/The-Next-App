import React from 'react';
// import useHooks from '@/hooks/useHooks';
import useHooks from '../../hooks/useHooks'; //Relative path

const SuperSection = () => {
    return (
        <div>
            Super Section
        </div>
    );
};

export default SuperSection;