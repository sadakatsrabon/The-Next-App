import React from 'react';
// Absoulute path for import
import useHooks from '@/hooks/useHooks';
//Relative path
// import useHooks from '../../hooks/useHooks';

const SuperSection = () => {
    return (
        <div>
            Super Section
        </div>
    );
};

export default SuperSection;